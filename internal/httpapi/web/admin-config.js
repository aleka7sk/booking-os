import {api} from './api.js';
import {icon,escapeHTML,money,dateTime,initials,modal,closePortal,toast,loading,empty} from './ui.js';
import {plural,durationText} from './admin-booking.js';

export async function renderResources(root,ctx){const [rs,os]=await Promise.all([api.resources(),api.offerings()]),pools=Object.groupBy?Object.groupBy(rs,r=>r.pool):rs.reduce((a,r)=>((a[r.pool]??=[]).push(r),a),{});root.innerHTML=`<main class="page"><div class="page-head compact"><div><small>ОГРАНИЧЕННАЯ ЁМКОСТЬ</small><h1>Ресурсы</h1><p>То, что система фактически резервирует.</p></div><button class="btn primary" id="add">${icon('plus')} Добавить ресурс</button></div><section class="resource-grid">${Object.entries(pools).map(([pool,items])=>`<article class="card"><div class="card-head"><div><h3>${escapeHTML(pool)}</h3><p>${items.length} ${plural(items.length,'ресурс','ресурса','ресурсов')} · ${os.filter(o=>o.resourcePool===pool).length} услуг</p></div><span class="count">${items.length}</span></div><div class="resource-list">${items.map(r=>`<div><i style="background:${escapeHTML(r.color)}"></i><span><b>${escapeHTML(r.name)}</b><small>${r.type} · вместимость ${r.capacity}</small></span><em>${r.active?'Активен':'Выключен'}</em></div>`).join('')}</div></article>`).join('')}</section></main>`;root.querySelector('#add').onclick=()=>resourceModal(ctx)}

export async function renderServices(root,ctx){const os=await api.offerings();root.innerHTML=`<main class="page"><div class="page-head compact"><div><small>КАТАЛОГ И ПРАВИЛА</small><h1>Услуги</h1><p>Как клиент выбирает время и как ядро занимает ресурсы.</p></div><button class="btn primary" id="add">${icon('plus')} Новая услуга</button></div><section class="service-grid">${os.map(o=>`<article class="service-card ${o.accent||'violet'}"><div class="service-top"><i>${icon(o.allocationMode==='SHARED_CAPACITY'?'users':'sparkles')}</i>${o.publicEnabled?'<span>Онлайн</span>':'<span class="muted">Скрыта</span>'}</div><h3>${escapeHTML(o.name)}</h3><p>${escapeHTML(o.description||'Описание не добавлено')}</p><div class="service-facts"><span><small>Формат</small><b>${o.schedulingMode==='FIXED_SESSION'?'Фиксированные сеансы':o.schedulingMode==='APPOINTMENT'?'Запись':'Аренда'}</b></span><span><small>Длительность</small><b>${durationText(o.durationMin)}</b></span><span><small>Цена</small><b>${money(o.basePrice)}</b></span><span><small>Подтверждение</small><b>${o.confirmationMode==='INSTANT'?'Мгновенно':o.confirmationMode==='PAYMENT_GATED'?'После оплаты':'Менеджером'}</b></span></div><footer><span>${escapeHTML(o.resourcePool)}</span><b>${o.depositPercent?`${o.depositPercent}% предоплата`:'без предоплаты'}</b></footer></article>`).join('')}</section></main>`;root.querySelector('#add').onclick=()=>serviceModal(ctx)}

export async function renderActivity(root){const es=await api.audit();root.innerHTML=`<main class="page"><div class="page-head compact"><div><small>AUDIT TRAIL</small><h1>История действий</h1><p>Значимые операции не исчезают и не перезаписываются.</p></div></div><section class="card timeline">${es.length?es.map(e=>`<div><i>${icon(e.action.includes('payment')?'wallet':e.action.includes('cancel')||e.action.includes('reject')?'x':'check')}</i><span><b>${escapeHTML(e.summary)}</b><small>${dateTime(e.createdAt)} · ${escapeHTML(e.actorId)}</small></span><em>${escapeHTML(e.action)}</em></div>`).join(''):empty('Действий пока нет','События появятся после работы с бронями.')}</section></main>`}
export async function renderSettings(root,ctx){const o=ctx.state.organization,l=ctx.state.locations[0];root.innerHTML=`<main class="page"><div class="page-head compact"><div><small>РАБОЧЕЕ ПРОСТРАНСТВО</small><h1>Настройки</h1><p>Конфигурация пилотной организации.</p></div></div><section class="settings-grid"><article class="card setting-card"><i class="setting-logo">${escapeHTML(initials(o.name))}</i><div><h3>${escapeHTML(o.name)}</h3><p>${escapeHTML(o.legalName||'')}</p></div><dl><dt>Точка</dt><dd>${escapeHTML(l?.name||'—')}</dd><dt>Адрес</dt><dd>${escapeHTML(l?.address||'—')}</dd><dt>Часовой пояс</dt><dd>${escapeHTML(l?.timezone||o.timezone)}</dd><dt>Валюта</dt><dd>${escapeHTML(o.currency)}</dd></dl></article><article class="card public-card"><small>${icon('external')} ПУБЛИЧНАЯ ЗАПИСЬ</small><h2>${escapeHTML(o.publicTitle)}</h2><p>${escapeHTML(o.publicSubtitle)}</p><div class="public-url"><code>${escapeHTML(location.origin+'/book')}</code><button id="copy">${icon('copy')}</button></div><button class="btn primary" id="open-public">Открыть страницу клиента ${icon('external')}</button></article><article class="card warning-card"><div>${icon('warning')}</div><span><h3>Контролируемый MVP</h3><p>Текущая версия использует атомарное файловое хранилище и рассчитана на одну реплику. Перед production необходим PostgreSQL.</p></span></article></section></main>`;root.querySelector('#open-public').onclick=()=>open('/book','_blank');root.querySelector('#copy').onclick=async()=>{await navigator.clipboard.writeText(location.origin+'/book');toast('Ссылка скопирована')}}

function resourceModal(ctx){modal({title:'Новый ресурс',sub:'Дорожка, комната, зал, сотрудник или оборудование',body:`<form id="resource-form" class="form"><label>Название<input name="name" required placeholder="Дорожка 5"></label><label>Пул ресурсов<input name="pool" required placeholder="BOWLING"></label><div class="form-grid"><label>Тип<select name="type"><option value="ASSET">Оборудование / объект</option><option value="SPACE">Помещение</option><option value="PERSON">Сотрудник</option><option value="EQUIPMENT">Инвентарь</option></select></label><label>Вместимость<input name="capacity" type="number" min="1" value="1"></label><label>Цвет<input name="color" type="color" value="#7868F2"></label></div><div class="form-error" hidden></div></form>`,footer:`<button class="btn secondary" data-close>Отмена</button><button class="btn primary" id="save">Добавить</button>`,onReady:m=>{const form=m.querySelector('form'),b=m.querySelector('#save'),err=m.querySelector('.form-error');b.onclick=async()=>{if(!form.reportValidity())return;const d=new FormData(form);loading(b,true);try{await api.createResource({name:d.get('name'),locationId:ctx.state.locations[0].id,pool:d.get('pool'),type:d.get('type'),capacity:+d.get('capacity'),color:d.get('color'),notes:''});closePortal();toast('Ресурс добавлен');await ctx.refresh()}catch(e){err.textContent=e.message;err.hidden=false;loading(b,false)}}}})}

function serviceModal(ctx){const weekdays=['Вс','Пн','Вт','Ср','Чт','Пт','Сб'];modal({title:'Новая услуга',sub:'Настройте время, ресурс, цену и способ подтверждения',wide:true,body:`<form id="service-form" class="form"><div class="form-grid"><label>Название<input name="name" required placeholder="Бильярд"></label><label>Категория<input name="category" placeholder="Активный отдых"></label><label class="full">Описание<textarea name="description"></textarea></label><label>Пул ресурсов<input name="resourcePool" required placeholder="BILLIARD"></label><label>Формат<select name="schedulingMode"><option value="RENTAL">Аренда с длительностью</option><option value="APPOINTMENT">Запись на услугу</option><option value="FIXED_SESSION">Фиксированные сеансы</option></select></label><label>Использование<select name="allocationMode"><option value="EXCLUSIVE">Ресурс целиком</option><option value="SHARED_CAPACITY">Общая вместимость</option></select></label><label>Подтверждение<select name="confirmationMode"><option value="MANAGER_APPROVAL">Менеджером</option><option value="INSTANT">Мгновенно</option><option value="PAYMENT_GATED">После предоплаты</option><option value="APPROVAL_AND_PAYMENT">Менеджер + предоплата</option></select></label><label>Цена<select name="priceMode"><option value="FIXED">Фиксированная</option><option value="PER_PERSON">За человека</option><option value="PER_RESOURCE">За ресурс</option><option value="PER_DURATION">За час</option><option value="PER_RESOURCE_DURATION">За ресурс / час</option></select></label><label>Базовая цена, ₸<input name="basePrice" type="number" min="0" value="10000"></label><label>Предоплата, %<input name="depositPercent" type="number" min="0" max="100" value="0"></label><label>Стандартная длительность<input name="durationMin" type="number" min="15" value="60"></label><label>Минимум минут<input name="minDurationMin" type="number" min="15" value="60"></label><label>Максимум минут<input name="maxDurationMin" type="number" min="15" value="240"></label><label>Шаг длительности<input name="durationStepMin" type="number" min="15" value="60"></label><label>Мин. гостей<input name="minGuests" type="number" min="1" value="1"></label><label>Макс. гостей<input name="maxGuests" type="number" min="1" value="12"></label><label>Вместимость одного ресурса<input name="capacityPerResource" type="number" min="1" value="6"></label><label>Буфер после, мин<input name="bufferAfterMin" type="number" min="0" value="15"></label><label>Предварительная запись, мин<input name="minLeadMin" type="number" min="0" value="30"></label><label>Горизонт, дней<input name="maxAdvanceDays" type="number" min="1" max="365" value="60"></label><label>Hold, минут<input name="holdDurationMin" type="number" min="1" value="15"></label><label class="check"><input name="publicEnabled" type="checkbox" checked> Доступна онлайн</label><label class="full">Правила отмены<textarea name="cancellationPolicy">Бесплатная отмена не позднее чем за 24 часа.</textarea></label></div><section id="fixed" hidden><h3>Повторяющиеся сеансы</h3><div class="week-chips">${weekdays.map((d,i)=>`<button type="button" data-day="${i}">${d}</button>`).join('')}</div><div class="fixed-time"><input type="time" value="19:00"><button type="button" class="btn secondary" id="add-time">${icon('plus')} Добавить время</button></div><div id="fixed-list"></div></section><div class="form-error" hidden></div></form>`,footer:`<button class="btn secondary" data-close>Отмена</button><button class="btn primary" id="save">Создать услугу</button>`,onReady:m=>{const form=m.querySelector('form'),fixed=m.querySelector('#fixed'),list=m.querySelector('#fixed-list'),err=m.querySelector('.form-error'),save=m.querySelector('#save');let days=new Set(),sessions=[];const draw=()=>list.innerHTML=sessions.map((s,i)=>`<span>${weekdays[s.weekday]} ${s.start}<button type="button" data-rm="${i}">${icon('x')}</button></span>`).join('');form.schedulingMode.onchange=()=>fixed.hidden=form.schedulingMode.value!=='FIXED_SESSION';m.querySelectorAll('[data-day]').forEach(b=>b.onclick=()=>{days.has(+b.dataset.day)?days.delete(+b.dataset.day):days.add(+b.dataset.day);b.classList.toggle('selected')});m.querySelector('#add-time').onclick=()=>{const v=m.querySelector('.fixed-time input').value;if(!days.size||!v)return toast('Выберите дни недели','Затем добавьте время',true);days.forEach(d=>{if(!sessions.some(s=>s.weekday===d&&s.start===v))sessions.push({weekday:d,start:v})});draw()};list.onclick=e=>{const b=e.target.closest('[data-rm]');if(b){sessions.splice(+b.dataset.rm,1);draw()}};save.onclick=async()=>{if(!form.reportValidity())return;const d=new FormData(form);loading(save,true);try{await api.createOffering({name:d.get('name'),locationId:ctx.state.locations[0].id,category:d.get('category'),description:d.get('description'),schedulingMode:d.get('schedulingMode'),allocationMode:d.get('allocationMode'),confirmationMode:d.get('confirmationMode'),priceMode:d.get('priceMode'),basePrice:+d.get('basePrice'),depositPercent:+d.get('depositPercent'),durationMin:+d.get('durationMin'),minDurationMin:+d.get('minDurationMin'),maxDurationMin:+d.get('maxDurationMin'),durationStepMin:+d.get('durationStepMin'),startStepMin:30,bufferBeforeMin:0,bufferAfterMin:+d.get('bufferAfterMin'),minGuests:+d.get('minGuests'),maxGuests:+d.get('maxGuests'),resourcePool:d.get('resourcePool'),resourcesRequired:1,capacityPerResource:+d.get('capacityPerResource'),minLeadMin:+d.get('minLeadMin'),maxAdvanceDays:+d.get('maxAdvanceDays'),holdDurationMin:+d.get('holdDurationMin'),publicEnabled:d.get('publicEnabled')==='on',accent:'violet',cancellationPolicy:d.get('cancellationPolicy'),fixedSessions:sessions});closePortal();toast('Услуга создана');await ctx.refresh()}catch(e){err.textContent=e.message;err.hidden=false;loading(save,false)}}}})}
